# Contributing

1. Fork the repository and create a focused branch.
2. Do not commit credentials, generated APKs, signing keys, or downloaded native binaries.
3. Run `pytest -q` before opening a pull request.
4. Explain behavior and security changes clearly in the pull request.
5. Keep the public API backward compatible unless the change is documented as breaking.
